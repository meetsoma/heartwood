# Local agent memory lives in the PARENT of this repo, not inside it.
# See the layout note in README.md — the split is structural, not rule-based,
# so there is deliberately no .soma/ entry here to enforce.

.DS_Store
*.tgz
node_modules/
